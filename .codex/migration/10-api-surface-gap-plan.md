# API Surface Gap Plan (Uncovered Controllers)

- Total uncovered controllers: 0

## Status
- Gap plan items have been merged into `02-granular-feature-catalog.md` section `10. API-only 覆盖矩阵（无独立页面）`.
- Use that section as the canonical migration backlog for API-only controllers.
